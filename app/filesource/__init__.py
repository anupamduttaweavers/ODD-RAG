"""
File Source Configuration and Access Layer.

Independent, modular abstraction that allows the application to fetch
documents from local directories, SMB/CIFS shares, SFTP servers, NFS
mounts, FTP(S) servers, and any authenticated network storage — without
modifying existing synchronization or vectorization logic.
"""
